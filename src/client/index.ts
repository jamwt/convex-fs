// Client API for the blob store component

// Re-export types for consumers
export type {
  Config,
  FileMetadata,
  Op,
  Dest,
} from "../component/validators.js";

// TODO: Add client helper functions for blob store operations
